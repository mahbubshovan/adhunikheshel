# আধুনিক হেঁশেল — ওয়েবসাইট আপডেট গাইড

এই গাইড আপনার বর্তমান ওয়েবসাইটের ফাইল ও কাজের ধরন অনুযায়ী লেখা।

প্রজেক্ট ফোল্ডার: `/Users/wpdev/Github/adhunik heshel`

## ১. এখন কোন কাজ কোথা থেকে করা যাবে

**অ্যাডমিন পেজে এখন অর্ডার দেখা, খোঁজা, স্ট্যাটাস পরিবর্তন এবং ইনভয়েস প্রিন্ট করা যায়। লেখা, ছবি, ভিডিও বা দাম পরিবর্তনের ফর্ম এখনো নেই।** এসব পরিবর্তন করতে প্রজেক্টের ফাইল বা পণ্যের ডাটাবেজ আপডেট করতে হবে। আপনি চাইলে নিচের নমুনার মতো পরিবর্তনগুলো আমাকে বা আপনার ডেভেলপারকে দিতে পারবেন।

| পরিবর্তন | বর্তমান জায়গা | লাইভে কার্যকর করার পদ্ধতি |
|---|---|---|
| হোমপেজের শিরোনাম, গল্প, বোতাম, যোগাযোগ | `app/storefront.tsx` | ফাইল পরিবর্তন করে নতুন সংস্করণ প্রকাশ |
| পণ্যের নাম, বিবরণ, ছবি কোথা থেকে আসবে, ক্যাটাগরি | ডাটাবেজের `products` টেবিল | নির্দিষ্ট লাইভ রেকর্ড আপডেট |
| পণ্যের ওজন ও দাম | ডাটাবেজের `variants` টেবিল | নির্দিষ্ট লাইভ রেকর্ড আপডেট |
| ছবি ও লোগোর ফাইল | `public/images/` | নতুন ফাইল ও প্রয়োজনীয় লিংকসহ প্রকাশ |
| ভিডিওর ফাইল | `public/videos/` | নতুন ফাইল ও প্রয়োজনীয় লিংকসহ প্রকাশ |
| ভিডিওর বোতাম, শিরোনাম, সময়, ফাইলের নাম | `app/product-video.tsx` | ফাইল পরিবর্তন করে প্রকাশ |
| ডেলিভারি চার্জ | `lib/catalog.ts` এবং হোমপেজের সংশ্লিষ্ট লেখা | হিসাব ও দৃশ্যমান লেখা একসঙ্গে পরিবর্তন করে প্রকাশ |
| রং, ফন্ট, সাইজ, মোবাইলের বিন্যাস | `app/globals.css` | ফাইল পরিবর্তন করে প্রকাশ |
| ব্রাউজার টাইটেল, সার্চের বিবরণ, আইকন | `app/layout.tsx` | ফাইল পরিবর্তন করে প্রকাশ |
| ইনভয়েস ও অ্যাডমিনের লেখা | `app/admin/page.tsx` | ফাইল পরিবর্তন করে প্রকাশ |
| অর্ডারের স্ট্যাটাস | `/admin` | সংরক্ষণ করলেই ডাটাবেজে কার্যকর |

এই টেবিলের ফাইলের নামগুলো প্রজেক্ট ফোল্ডারের ভেতরের ঠিকানা। লাইভ সাইট চালু হলে `/admin` হবে আপনার ডোমেইনের শেষে `/admin`।

## ২. লেখা পরিবর্তন

হোমপেজে “এক বয়ামে ঘরের ভালোবাসা”, “আপনার পছন্দের স্বাদ”, আমাদের গল্প, অর্ডারের নির্দেশনা বা ফুটারের লেখা পরিবর্তন করতে `app/storefront.tsx` ব্যবহার করতে হবে। ভিডিও সেকশনের লেখা `app/product-video.tsx`-এ আছে।

পরিবর্তন জানানোর সময় তিনটি জিনিস দিন:

1. কোথায় পরিবর্তন চান—যেমন হোমপেজের বড় শিরোনাম।
2. এখন কী লেখা আছে।
3. নতুন করে ঠিক কী লিখতে চান।

বাংলা বানান হুবহু দিন। বিশেষ করে “হেঁশেল”, “শুঁটকি”, “ঘরোয়া” এবং পণ্যের নাম মিলিয়ে দেখুন। কোড নিজে সম্পাদনা করলে ট্যাগ, বন্ধনী ও উদ্ধৃতিচিহ্ন অক্ষত রেখে শুধু দৃশ্যমান লেখাটি পরিবর্তন করুন।

**পণ্যের কার্ডের নাম ও বিবরণ আলাদা:** এগুলো হোমপেজের ফাইলে লেখা নেই; `products` টেবিল থেকে আসে। নতুন ক্যাটাগরি যোগ করলে হোমপেজের ফিল্টারের তালিকাও আপডেট করতে হবে।

## ৩. পণ্যের ছবি পরিবর্তন

বর্তমান ছবির তালিকা:

| পণ্য | পণ্যের ID | বর্তমান ছবি |
|---|---|---|
| ছুরি শুঁটকি | `churi` | `public/images/churi-product.png` |
| লইট্টা শুঁটকি | `loitta` | `public/images/loitta-product.png` |
| পুঁটি চ্যাপা শুঁটকি | `chepa` | `public/images/chepa-product.png` |
| ইলিশের আচার | `ilish` | `public/images/ilish-product.png` |

একই নাম ও একই ফরম্যাটে ছবি বদলালে নতুন ফাইল প্রকাশ করতে হবে। লাইভে পুরোনো ছবি ক্যাশে থেকে যেতে পারে। তাই **নতুন সংস্করণের জন্য নতুন নাম ব্যবহার করা ভালো**, যেমন `churi-product-v2.webp`। এরপর `products.image`-এ `/images/churi-product-v2.webp` বসাতে হবে।

ছবির ওয়েব ঠিকানায় `public` লিখবেন না। উদাহরণ: ফাইল `public/images/churi-product-v2.webp`, কিন্তু সাইটের ঠিকানা `/images/churi-product-v2.webp`।

নতুন ছবি আগে লাইভে প্রকাশ করে সেটির ঠিকানা খুলে দেখুন; তারপর ডাটাবেজে ওই ঠিকানা বসান। এতে মাঝখানে ভাঙা ছবি দেখানোর সম্ভাবনা কমে। পুরোনো ফাইলটি সঙ্গে সঙ্গে সরাবেন না—প্রয়োজনে ফিরে যেতে কাজে লাগবে।

ছবি প্রস্তুতের সময়:

- বর্তমান কার্ডের জন্য ৪:৫ অনুপাত মানানসই, যেমন ১১২০ × ১৪০০ পিক্সেল।
- বয়ামের নাম ও লেবেল যেন মোবাইলেও পড়া যায়।
- ছবি পরিষ্কার রেখে ফাইলের আকার কমান; সম্ভব হলে প্রতি ছবি প্রায় ২০০–৫০০ KB রাখুন। এটি লক্ষ্য, বাধ্যতামূলক সীমা নয়।
- শুধু ফাইলের নামের `.png` বদলে `.webp` লিখলে ফরম্যাট পরিবর্তন হয় না; সঠিকভাবে এক্সপোর্ট করতে হবে।
- কার্ড ও ব্যাগ একই পণ্যের ছবির ঠিকানা ব্যবহার করে, তাই দুটো জায়গাই পরীক্ষা করুন।

## ৪. লোগো, হিরো ছবি ও ভিডিওর কভার

| বিষয় | ফাইল | কোথায় ব্যবহার |
|---|---|---|
| ব্র্যান্ড লোগো | `public/images/logo.png` | হেডার, অ্যাডমিন, ইনভয়েস ও ব্রাউজার আইকন |
| হোমপেজের বড় রান্নার ছবি | `public/images/churi-cooked.jpg` | হোমপেজের প্রথম অংশ |
| সব পণ্যের ভিডিওর কভার | `public/images/collection-video-poster.jpg` | ভিডিও চালানোর আগের ছবি |

লোগোর নতুন নাম দিলে `app/storefront.tsx`, `app/admin/page.tsx` ও `app/layout.tsx`-এ ব্যবহারগুলো মিলিয়ে পরিবর্তন করুন। বিকল্প favicon আছে `public/favicon.svg`-এ; সেটিও ব্র্যান্ডের সঙ্গে মিলিয়ে রাখুন।

হিরো ছবির ঠিকানা `app/storefront.tsx`-এ এবং ভিডিওর কভারের ঠিকানা `app/product-video.tsx`-এ আছে।

## ৫. ভিডিও পরিবর্তন

| পণ্য | বর্তমান ভিডিও |
|---|---|
| ছুরি | `public/videos/01-churi-shutki.mp4` |
| লইট্টা | `public/videos/02-loitta-shutki.mp4` |
| পুঁটি চ্যাপা | `public/videos/03-puti-chapa-shutki.mp4` |
| ইলিশের আচার | `public/videos/04-ilisher-achar.mp4` |
| সব পণ্য একসঙ্গে | `public/videos/adhunik-heshel-all-products-cinematic.mp4` |

আলাদা পণ্যের ভিডিওগুলোর নাম `app/product-video.tsx`-এর `videos` তালিকায় আছে। সব পণ্য মিলিয়ে ভিডিওটির ঠিকানা একই ফাইলের `CollectionVideo` অংশে আছে; ভিডিও চালু না হলে যে বিকল্প লিংক দেখায়, সেটিও পরিবর্তন করুন।

নতুন ভিডিও বসানোর নিয়ম:

1. ভিডিওটি আগে সম্পূর্ণ চালিয়ে পণ্য, বাংলা লেখা, দাম ও শব্দ মিলিয়ে নিন।
2. নতুন ফাইলটি `public/videos/`-এ দিন। লাইভ আপডেটে নতুন নাম ব্যবহার করুন, যেমন `01-churi-shutki-v2.mp4`।
3. সংশ্লিষ্ট ফাইলের নাম বা ঠিকানা পরিবর্তন করুন।
4. ভিডিওর দৈর্ঘ্য বদলালে “৭ সেকেন্ড” বা “প্রায় ১৫ সেকেন্ড” লেখাও বদলান; এগুলো এখন স্বয়ংক্রিয় নয়। আলাদা দৈর্ঘ্যের ভিডিও হলে প্রতিটি পণ্যের জন্য সময় আলাদা করে রাখতে হবে।
5. সব পণ্যের ভিডিও বদলালে কভার ছবিটিও মিলিয়ে নিন।
6. ফোনে প্লে, পজ, শব্দ ও ফুলস্ক্রিন পরীক্ষা করুন।

বর্তমান ভিডিওগুলো ৯:১৬ অনুপাতের। নতুন ভিডিওতে একই অনুপাত রাখলে বর্তমান বিন্যাসে ভালো মানাবে। ওয়েবে ব্যবহারের জন্য MP4-তে H.264 ভিডিও ও AAC অডিও একটি ব্যবহারযোগ্য এক্সপোর্ট পছন্দ।

ভিডিওগুলো নিজে থেকে শব্দসহ চালু হয় না। ব্যবহারকারী প্লে করলে শুরু হয়, অন্য ভিডিও চললে সেটি থামে। এই আচরণ বজায় রাখুন।

**বর্তমান একক ভিডিওগুলোর ২ ও ৫ সেকেন্ডের নমুনায় শুধু ঝাপসা ব্যাকগ্রাউন্ড দেখা গিয়েছিল। লাইভ করার আগে চারটি ভিডিও সম্পূর্ণ দেখে প্রয়োজনে সংশোধিত ফাইল দিন।** ভিডিওতে দাম লেখা থাকলে ওয়েবসাইটের দাম পরিবর্তনের সঙ্গে সেই দামও মিলিয়ে নিন।

## ৬. দাম, ওজন ও পণ্যের বিবরণ

পণ্যের দাম `variants.price`-এ পূর্ণ টাকায় রাখা হয়: ২৫০ টাকা হলে `250`, পয়সা হিসেবে `25000` নয়।

ওজন `variants.grams`-এ এবং পণ্যের নাম/বিবরণ `products`-এ থাকে। যেমন `churi-100` হলো ছুরি শুঁটকির ১০০ গ্রামের মূল্য-রেকর্ড।

**শুধু `db/seed.sql` সম্পাদনা করে পুরোনো পণ্যের দাম বদলাবে না।** এই ফাইল `INSERT OR IGNORE` ব্যবহার করে—নতুন রেকর্ড যোগ করে, থাকা রেকর্ড আপডেট করে না। বিদ্যমান ডাটাবেজে নির্দিষ্ট রেকর্ড আপডেট করতে হবে। ভবিষ্যতের নতুন সেটআপে সঠিক দাম রাখার জন্য seed ফাইলটিও মিলিয়ে রাখা যায়।

ডেভেলপারের জন্য উদাহরণ—শুধু যদি ছুরি ১০০ গ্রামের দাম ২৮০ টাকা করার নির্দেশ দেওয়া হয়:

```sql
SELECT id, price FROM variants WHERE id = 'churi-100';
UPDATE variants SET price = 280 WHERE id = 'churi-100';
SELECT id, price FROM variants WHERE id = 'churi-100';
```

এটি উদাহরণমাত্র; গাইড তৈরির সময় কোনো দাম বদলানো হয়নি। চালানোর আগে লোকাল না লাইভ—কোন ডাটাবেজে কাজ হচ্ছে তা নিশ্চিত করতে হবে।

পুরোনো অর্ডারের পণ্যের নাম, ওজন ও দাম `order_items`-এ আলাদা করে সংরক্ষিত থাকে। ক্যাটালগের দাম পরিবর্তন করলে আগের অর্ডারের বিল বদলায় না। তবে দাম পরিবর্তনের সময় কারও আগে থেকে খোলা ব্যাগ থাকলে checkout-এর হিসাব সার্ভারের সর্বশেষ দাম থেকে হবে; প্রকাশের পরে নতুন করে পেজ খুলে মোট মূল্য মিলিয়ে দেখুন।

পণ্য সাময়িক বন্ধ করতে `products.active = 0` করুন; পুনরায় দেখাতে `1`। পুরোনো অর্ডারের সঙ্গে সম্পর্ক থাকায় পণ্য বা variant মুছে দেওয়ার বদলে নিষ্ক্রিয় রাখুন।

## ৭. নতুন পণ্য বা নতুন ওজন যোগ করা

নতুন পণ্যের জন্য একটি নতুন, স্থায়ী ID, নাম, বিবরণ, ক্যাটাগরি, ছবি এবং অন্তত একটি ওজন-দাম দরকার। নতুন ID পুরোনো কোনো পণ্যের জন্য পুনর্ব্যবহার করবেন না।

ডেভেলপারকে দিতে হবে:

- পণ্যের বাংলা নাম ও সংক্ষিপ্ত বিবরণ।
- প্রতিটি ওজনের দাম।
- পণ্যের ছবি এবং থাকলে ভিডিও।
- কোন ক্যাটাগরিতে থাকবে।

ডাটাবেজে পণ্য ও variant যোগ হলে কার্ড তৈরি হয়। নতুন ক্যাটাগরি হলে ফিল্টার, ভিডিও থাকলে `videos` তালিকা, এবং “চার রকম স্বাদ” ধরনের নির্দিষ্ট সংখ্যা লেখা থাকলে সেগুলোও সংশোধন করতে হবে।

## ৮. ডেলিভারি চার্জ পরিবর্তন

বর্তমান চার্জ: ঢাকার ভিতরে ৮০ টাকা, বাইরে ১২০ টাকা।

মূল হিসাব `lib/catalog.ts`-এর `deliveryFees` থেকে আসে। কিন্তু উপরের বার, checkout-এর এলাকার তালিকা, ব্যাগের তথ্য ও অর্ডারের নির্দেশনায় চার্জের লেখা `app/storefront.tsx`-এ আলাদাভাবে লেখা আছে। **দুটোই একই আপডেটে বদলাতে হবে।**

শুধু উপরের লেখা বদলালে অর্ডারের হিসাব বদলাবে না। শুধু হিসাব বদলালে ক্রেতা ভুল চার্জ লেখা দেখতে পারেন।

## ৯. ফোন, WhatsApp ও Facebook পরিবর্তন

হোমপেজের দৃশ্যমান নম্বর, `tel:` লিংক, WhatsApp লিংক এবং Facebook লিংক `app/storefront.tsx`-এ আছে। ইনভয়েসের নম্বর আছে `app/admin/page.tsx`-এ। সব ব্যবহার একসঙ্গে খুঁজে পরিবর্তন করুন।

বর্তমান ব্যবসার নম্বর: `01672-777204`। আন্তর্জাতিক লিংকে ব্যবহৃত হয় `8801672777204`। নতুন নম্বর দিলে লেখা ও ক্লিক করে যাওয়ার ঠিকানা দুটো মিলিয়ে নিন।

## ১০. অর্ডার স্ট্যাটাস ও নগদ সংগ্রহ

অ্যাডমিনে অর্ডারের নিচে “স্ট্যাটাস পরিবর্তন” থেকে অবস্থা বেছে “সংরক্ষণ করুন” চাপুন।

- ডেলিভারি সম্পন্ন করলে নগদ সংগ্রহও সম্পন্ন হিসেবে গণনা হয়। হাতে টাকা পাওয়ার তথ্য নিশ্চিত করে এই স্ট্যাটাস দিন।
- ডেলিভারি সম্পন্ন অর্ডার অন্য স্ট্যাটাসে ফেরালে বর্তমান সিস্টেম নগদ সংগ্রহকে আবার অপরিশোধিত করে।
- শুধু স্ট্যাটাসের বাংলা নাম বদলাতে হলে `lib/catalog.ts`-এর `statusLabels` বদলান।
- নতুন ধরনের স্ট্যাটাস যোগ করতে হলে API, অ্যাডমিন ও হিসাবের নিয়ম একসঙ্গে পরিবর্তন করতে হবে।

স্ট্যাটাস পরিবর্তনের জন্য সাইট নতুন করে প্রকাশ করতে হয় না।

## ১১. একটি আপডেট লাইভে দেওয়ার নিয়ম

**কম্পিউটারে ফাইল বদলানো মানেই লাইভ সাইট বদলানো নয়।** কোড/মিডিয়ার পরিবর্তন প্রকাশ করতে হয়; লোকাল ডাটাবেজের পরিবর্তনও লাইভ ডাটাবেজে স্বয়ংক্রিয়ভাবে যায় না।

1. কী পরিবর্তন করবেন এবং পুরোনো মান কী ছিল লিখে রাখুন।
2. বর্তমান সোর্সের সংস্করণ সংরক্ষণ করুন। ডাটাবেজ বদলালে তার ব্যাকআপও নিন।
3. লোকাল বা পরীক্ষার সাইটে পরিবর্তন করুন।
4. লেখা, ছবি, ভিডিও ও মোবাইলের বিন্যাস দেখুন।
5. দাম/ডেলিভারি/checkout বদলালে ব্যাগ, দুই এলাকার মোট মূল্য, অর্ডার এবং ইনভয়েস পরীক্ষা করুন।
6. পরীক্ষিত সোর্স ও নতুন মিডিয়া হোস্টিংয়ে প্রকাশ করুন। ডাটাবেজের প্রয়োজনীয় পরিবর্তন আলাদাভাবে নির্দিষ্ট লাইভ ডাটাবেজে করুন।
7. লাইভ ডোমেইন নতুন করে খুলে মিলিয়ে দেখুন।
8. প্রকাশের সময়, পরিবর্তন এবং আগের সংস্করণে ফেরার উপায় লিখে রাখুন।

ডেভেলপারের লোকাল যাচাই:

```sh
cd '/Users/wpdev/Github/adhunik heshel'
npx tsc --noEmit
npm run build
npm test
```

`npm test` আলাদা পরীক্ষার ডাটাবেজ ব্যবহার করে। ছোট লেখা বা ছবি পরিবর্তনে সব অর্ডার পরীক্ষা বারবার চালানো জরুরি নয়; checkout, দাম, ডেলিভারি বা অর্ডারের নিয়ম বদলালে চালাবেন।

`npm run build` শুধু সাইট তৈরি করে, প্রকাশ করে না। বর্তমান `npm start`-ও Wrangler-এর লোকাল সার্ভার চালায়; এটি লাইভে প্রকাশের কমান্ড নয়।

## ১২. প্রথমবার লাইভ করার সময় যেগুলো সেট করতে হবে

বর্তমান প্রজেক্টে লোকাল ডাটাবেজ এবং লোকাল কনফিগারেশন আছে। প্রকৃত লাইভ ডাটাবেজ, ডোমেইন ও প্রকাশের প্রক্রিয়া এখনো সেট করা হয়নি। শুধু `public` ফোল্ডার আপলোড করলে checkout ও admin চলবে না; সার্ভার অংশ এবং ডাটাবেজ সংযোগ দরকার।

লাইভ সেটআপে সংরক্ষণ করুন:

- ব্যবহৃত হোস্টিংয়ের নাম, লাইভ URL ও সোর্স প্রজেক্ট।
- প্রকৃত ডাটাবেজের পরিচয় এবং `DB` সংযোগ।
- নতুন সংস্করণ প্রকাশ ও আগের কোডে ফেরার নির্দিষ্ট পদ্ধতি।
- ব্যাকআপ নেওয়া ও পুনরুদ্ধারের পদ্ধতি।
- ডোমেইন ও HTTPS সেটআপ।

এই প্রজেক্টের `.openai/hosting.json`-এ `DB` হলো সংযোগের নাম; `vite.config.ts` ও `wrangler.local.json`-এর placeholder database ID-কে প্রকৃত লাইভ ডাটাবেজ ধরে ব্যবহার করবেন না।

প্রথমবার লাইভে পণ্যের তথ্য বসাতে হবে। লোকালে বাস্তব অর্ডার থাকলে সেগুলো নিতে হবে কি না আলাদাভাবে ঠিক করুন। পরীক্ষার অর্ডার বা লোকাল লগইন সেশন লাইভে নেওয়ার প্রয়োজন নেই।

হোস্টিং নির্বাচন না হওয়ায় এই গাইডে কোনো অনুমানভিত্তিক লাইভ deploy কমান্ড দেওয়া হয়নি। সেটআপের পরে এই অংশে প্রকৃত প্রক্রিয়াটি যোগ করুন।

## ১৩. পাসওয়ার্ড ও ব্যাকআপ

লোকাল পাসওয়ার্ড `.dev.vars`-এর `ADMIN_PASSWORD`-এ থাকে। লাইভের জন্য **আলাদা নতুন পাসওয়ার্ড** রাখুন; চ্যাটে শেয়ার করা লোকাল পাসওয়ার্ডটি লাইভে ব্যবহার করবেন না।

Cloudflare ব্যবহার করলে লাইভ পাসওয়ার্ড Worker secret হিসেবে রাখতে হবে; সোর্স বা `public` ফোল্ডারে নয়। লোকাল `.dev.vars` গোপন রাখুন এবং Git-এ আপলোড করবেন না। [Cloudflare secrets নির্দেশনা](https://developers.cloudflare.com/workers/configuration/secrets/)

বর্তমান লগইন সেশন সর্বোচ্চ ১২ ঘণ্টা থাকে। ADMIN_PASSWORD বদলে নতুন সংস্করণ চালু করলে আগের পাসওয়ার্ডের সব সেশন বাতিল হবে।

লোকাল ব্যাকআপ: সার্ভার বন্ধ করে `.wrangler/state` সম্পূর্ণ ফোল্ডার একটি আলাদা তারিখযুক্ত স্থানে কপি করুন। সোর্সের ব্যাকআপের সঙ্গে এই ডাটাবেজের ব্যাকআপ আলাদাভাবে রাখতে হবে।

লাইভ D1 ব্যবহার করলে ডাটাবেজ SQL আকারে export করা যায়। বড় ডাটাবেজ পরিবর্তনের আগে বর্তমান লাইভ ডাটাবেজ export করে রাখুন। Export চলাকালে অন্য ডাটাবেজ অনুরোধ আটকে থাকতে পারে, তাই কম ব্যস্ত সময় বেছে নিন। [D1 import/export নির্দেশনা](https://developers.cloudflare.com/d1/best-practices/import-export-data/)

কোডের আগের সংস্করণে ফিরে গেলে ডাটাবেজ নিজে থেকে আগের অবস্থায় যায় না। পুরোনো সম্পূর্ণ ডাটাবেজ ফিরিয়ে দিলে ব্যাকআপের পরে আসা অর্ডার হারাতে পারে। তাই সমস্যা হলে আগে নির্দিষ্ট পরিবর্তন সংশোধন করার চেষ্টা করুন; পুরো restore করার আগে নতুন অর্ডার সংরক্ষণের পরিকল্পনা করুন।

## ১৪. আমাকে আপডেট দিতে সহজ নমুনা

এই তথ্যগুলো একসঙ্গে পাঠালে পরিষ্কারভাবে কাজ করা যাবে:

> আধুনিক হেঁশেল আপডেট করো।
> কাজের স্থান: লোকাল / পরীক্ষার সাইট / লাইভ সাইট।
> ১. হোমপেজের শিরোনাম হবে: [হুবহু লেখা]
> ২. ছুরি ১০০ গ্রামের নতুন দাম: [টাকা]
> ৩. ছুরির নতুন ছবি: [সংযুক্ত ফাইল]
> ৪. সব পণ্যের নতুন ভিডিও: [সংযুক্ত ফাইল]
> ৫. ডেলিভারি চার্জ: ঢাকায় [টাকা], বাইরে [টাকা]
> ৬. কখন কার্যকর হবে: [সময়]
> ৭. শুধু প্রিভিউ দেখাবে / যাচাই শেষে লাইভে প্রকাশ করবে।

যে বিষয় পরিবর্তন করতে চান না, তা তালিকায় দেওয়ার দরকার নেই। ফাইল দিলে কোন পণ্যের জন্য তা লিখে দিন।


## লাইভে যাওয়ার আগে নিরাপত্তা ও SEO (১১ সেপ্টেম্বর ২০২৬)

মূল ডোমেইন: `https://adhunikheshel.com`। হোমপেজের canonical, sitemap ও robots এই ডোমেইন ব্যবহার করে। ডোমেইন বদলালে `app/page.tsx`, `app/layout.tsx`, `app/robots.txt/route.ts`, `app/sitemap.xml/route.ts` আপডেট করুন। পণ্যের নাম ও দাম এখন সার্ভার থেকে প্রাথমিক HTML-এ আসে। অ্যাডমিন ও API-তে noindex এবং private/no-store আছে।

- হোস্টিং এখনো নিশ্চিত নয়। এই প্রজেক্ট Cloudflare Workers + D1-এর জন্য তৈরি; সাধারণ static hosting-এ অর্ডার/অ্যাডমিন চলবে না। আসল production D1 binding এবং migration লাগবে; placeholder database ID দিয়ে প্রকাশ করবেন না।
- লাইভে আলাদা random ADMIN_PASSWORD secret ব্যবহার করুন (অন্তত ১৬ অক্ষর, ২৪ বা বেশি ভালো)। `.dev.vars` বা লোকাল database আপলোড করবেন না। পাসওয়ার্ড বদলালে পুরোনো সেশন বাতিল হয়। আগে তৈরি সেশনও এই আপডেটের পরে আবার লগইন চাইবে।
- HTTPS চালু ও HTTP→HTTPS redirect নিশ্চিত করুন। অ্যাডমিন localhost ছাড়া HTTP-তে কাজ করবে না। HTTPS response-এ HSTS আছে; অন্য subdomain স্বয়ংক্রিয়ভাবে অন্তর্ভুক্ত নয়।
- www থেকে মূল ডোমেইনে redirect দিন। staging সাইট সার্চে খোলা রাখবেন না। Google Search Console-এ ডোমেইন verify করে `/sitemap.xml` জমা দিন; URL Inspection দিয়ে indexing দেখুন।
- Production database-এর নিয়মিত backup ও restore পরীক্ষা করুন। লাইভে একটি পরীক্ষার COD অর্ডার দিয়ে ঢাকা ৮০/বাইরে ১২০ টাকা, অ্যাডমিন login/status/logout যাচাই করুন। পরীক্ষার অর্ডার বাতিল করুন।
- IP rate limit Cloudflare-এর trusted client-IP header ধরে লেখা। অন্য হোস্টিংয়ে নেওয়ার আগে proxy trust ও rate limit বদলাতে হবে। বেশি bot/spam হলে edge rate limiting বা challenge যোগ করতে হবে।
- নিরাপত্তা header-এ framing, object embedding ও form destination সীমা আছে। এটি সম্পূর্ণ script-source CSP নয়। Inline script-এর nonce-সহ কঠোর CSP আলাদা browser পরীক্ষা করে যোগ করা যাবে।
- ছবিগুলো WebP/AVIF-এ ছোট করা, প্রতিটি পণ্যের নিজস্ব বিস্তারিত URL এবং সঠিক Product structured data SEO-র পরবর্তী উন্নতি। এখন এগুলোর rich-result যোগ্যতা দাবি করা হচ্ছে না। ভিডিও preload বন্ধ রাখা আছে।

এই পরীক্ষায় লাইভ ডোমেইন DNS resolve হয়নি; লাইভ TLS, redirect, caching, indexing ও production database যাচাই বাকি। স্থানীয় build/integration পরীক্ষা live deployment যাচাইয়ের বিকল্প নয়।

নির্দেশনার সূত্র: [Google JavaScript SEO](https://developers.google.com/search/docs/crawling-indexing/javascript/javascript-seo-basics), [Google robots metadata](https://developers.google.com/search/docs/crawling-indexing/robots-meta-tag), [OWASP HTTP Headers](https://cheatsheetseries.owasp.org/cheatsheets/HTTP_Headers_Cheat_Sheet.html)।

Dependency scan: production/runtime-এ ০টি পরিচিত advisory; development tooling-এ drizzle-kit → esbuild-kit → পুরোনো esbuild-এর একই advisory থেকে ৪টি moderate finding আছে। এই development server/tooling প্রকাশ্যে চালাবেন না। Breaking downgrade করে audit চুপ করানো হয়নি। ভবিষ্যতে compatible upstream fix দিয়ে আবার audit করুন।
