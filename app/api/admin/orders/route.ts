import {getDb} from '../../../../db';
import {body,json,sameOrigin,failure,HttpError,requireAdmin} from '../../../../lib/server';
export async function GET(req:Request){try{await requireAdmin(req);const orders=await getDb().prepare('SELECT id,customer_name,phone,address,area,notes,subtotal,delivery,total,payment_method,payment_status,status,created_at FROM orders ORDER BY created_at DESC').all();const items=await getDb().prepare('SELECT order_id,name,grams,quantity,unit_price FROM order_items').all();return json({orders:orders.results.map(o=>({...o,items:items.results.filter(i=>i.order_id===o.id)}))})}catch(e){return failure(e)}}
const statuses=['pending','confirmed','preparing','shipped','delivered','cancelled'];
export async function PATCH(req:Request){try{sameOrigin(req);await requireAdmin(req);const{id,status,expectedStatus}=await body(req);if(typeof id!=='string'||typeof status!=='string'||!statuses.includes(status))throw new HttpError(400,'স্ট্যাটাস সঠিক নয়।');const db=getDb();const order=await db.prepare('SELECT status FROM orders WHERE id=?').bind(id).first<{status:string}>();if(!order)throw new HttpError(404,'অর্ডার পাওয়া যায়নি।');if(expectedStatus!==undefined&&expectedStatus!==order.status)throw new HttpError(409,'অর্ডার পরিবর্তিত হয়েছে। রিফ্রেশ করে আবার চেষ্টা করুন।');const result=await db.prepare('UPDATE orders SET status=?,payment_status=? WHERE id=? AND status=?').bind(status,status==='delivered'?'collected':'unpaid',id,order.status).run();if(!result.meta.changes)throw new HttpError(409,'অর্ডার পরিবর্তিত হয়েছে। রিফ্রেশ করুন।');return json({ok:true})}catch(e){return failure(e)}}
export async function DELETE(req:Request){try{sameOrigin(req);await requireAdmin(req);const{id}=await body(req);if(typeof id!=='string')throw new HttpError(400,'অর্ডার আইডি সঠিক নয়।');const db=getDb();const order=await db.prepare('SELECT id FROM orders WHERE id=?').bind(id).first();if(!order)throw new HttpError(404,'অর্ডার পাওয়া যায়নি।');await db.batch([db.prepare('DELETE FROM order_items WHERE order_id=?').bind(id),db.prepare('DELETE FROM orders WHERE id=?').bind(id)]);return json({ok:true})}catch(e){return failure(e)}}
